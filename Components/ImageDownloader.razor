<MudFab Color="Color.Info" StartIcon="@Icons.Material.Filled.Save"
        Class="d-flex align-center justify-center mud-width-full" OnClick="GenerateImage" Label="Download Image" />
@code {
    [Inject]
    public IJSRuntime JSRuntime { get; set; }

    [Parameter, EditorRequired]
    public string IDOfImageToDownload { get; set; } = "ImageToDownload";

    private async Task GenerateImage()
    {
        await JSRuntime.InvokeVoidAsync("generateImage", IDOfImageToDownload);
    }
}
