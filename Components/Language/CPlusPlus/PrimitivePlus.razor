@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-cilkcpp">int myInteger = 10; // Used to store integer values.
float myFloat = 3.14f; // Used to store single-precision floating-point numbers.
double myDouble = 3.14159; // Used to store double-precision floating-point numbers.
bool isTrue = true; // Used to store boolean values (true or false).
char myChar = 'A'; // Used to store a single character.
unsigned char myByte = 255; // Used to store unsigned integers from 0 to 255.
short myShort = 1000; // Used to store small integers.
long myLong = 1000000000; // Used to store large integers.
long double myDecimal = 123.456; // Used to precise decimal numbers with a wide range.</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
