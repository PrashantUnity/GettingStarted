@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-go">package main

import "fmt"

func main() {
    var myNumber int                      // Declaring an integer variable without initializing it
    var greeting string = "Hello, World!" // Declaring a string variable and initializing it with a value
    var a, b, c int                       // Declaring multiple integer variables in a single line

    fmt.Println("myNumber:", myNumber)
    fmt.Println("greeting:", greeting)
    fmt.Println("a:", a, "b:", b, "c:", c)
}
</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
