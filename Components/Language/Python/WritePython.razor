@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-python">print("This will write in single line after that what ever you write will be printed in new Line")
print("We can use This to write in single line by multiple print() Statement", end=' ')
print("This Will Also Print In Same Line as Above")</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
