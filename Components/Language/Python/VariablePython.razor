@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-python">myNumber = None  # Declaring an integer variable without initializing it
greeting = "Hello, World!"  # Declaring a string variable and initializing it with a value
a = b = c = 0  # Declaring and initializing multiple integer variables in a single line</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
