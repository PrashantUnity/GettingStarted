@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-python">name = input("Enter your name: ")  # Prompt the user for input and store it in the variable 'name'
print(name)  # Print the value stored in the 'name' variable
</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
