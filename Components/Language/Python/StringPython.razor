@inject IJSRuntime JSRuntime
<pre class="line-numbers">
<code class="language-python"># String Concatenation
name = "John"
age = 30
message = "My name is " + name + " and I am " + str(age) + " years old."

# String Interpolation
name = "John"
age = 30
message = f"My name is {name} and I am {age} years old."

# Verbatim String (Multiline String in Python)
path = r'C:\MyFolder\MyFile.txt'
multiline = """This is a
               multiline
               string."""</code>
</pre>


@code {
    // Inside a .razor file or a separate class
    @inject IJSRuntime JSRuntime


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializePrism");
        }
    }

}
